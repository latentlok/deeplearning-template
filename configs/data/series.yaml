_target_: dataset.examples.SeriesData
length: 2000
window: 16 # must match model.window
horizons: [8, 24] # val_dataloader returns a dict -> val/h8/mae, val/h24/mae
train_frac: 0.7 # TEMPORAL split; never shuffled before splitting
batch_size: 32
noise: 0.05
num_workers: 0
seed: 0
