"""dlt -- a minimal, fork-first PyTorch training/tracking template.

Rename this package per fork; `core/` is what you never rewrite, `project/` is what
you replace immediately.
"""

__version__ = "0.1.0"
