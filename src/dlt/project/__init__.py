"""Your code. Each file is self-contained -- model, data and loss together -- so you
delete the examples you do not need instead of editing three files.

Shipped:
  mlp.py       abstract two-layer net on random tensors; the fast smoke/contract test
  forecast.py  windowed timeseries forecasting; the timeseries patterns that bite
  pinn.py      du/dx = -u with a gradient loss and adaptive term weighting
"""
